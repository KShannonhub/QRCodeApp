import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cubeyscans.app',
  appName: 'Cubey Scans',
  webDir: 'dist/qr-code-app/browser'
};

export default config;
